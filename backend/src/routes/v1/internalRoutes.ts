/**
 * @summary
 * Internal (authenticated) API routes configuration for V1.
 * Handles protected endpoints requiring authentication.
 *
 * @module routes/v1/internalRoutes
 */
import { Router } from 'express';

const router = Router();

// Internal routes will be added here as features are implemented
// Example: router.use('/weather', authMiddleware, weatherRoutes);

export default router;
